@using WebApplication1.ViewModels
@model UserProfileViewModel

@{
    ViewData["Title"] = "Đang theo dõi";
}

<div class="container py-4">
    <div class="row mb-4">
        <div class="col-md-8 offset-md-2">
            <div class="d-flex align-items-center mb-4">
                <a href="@Url.Action("Profile", "User", new { id = Model.User.Id })" class="btn btn-link">
                    <i class="bi bi-arrow-left"></i> Quay lại
                </a>
                <h2 class="ms-3 mb-0">Đang theo dõi</h2>
            </div>

            @if (Model.Following != null && Model.Following.Any())
            {
                <div class="list-group">
                    @foreach (var following in Model.Following)
                    {
                        <div class="list-group-item list-group-item-action">
                            <div class="d-flex w-100 justify-content-between align-items-center">
                                <div class="d-flex align-items-center">
                                    @if (!string.IsNullOrEmpty(following.Avatar))
                                    {
                                        <img src="@following.Avatar" alt="@following.DisplayName" class="rounded-circle me-3" width="40" height="40" />
                                    }
                                    else
                                    {
                                        <div class="rounded-circle bg-secondary text-white d-flex align-items-center justify-content-center me-3" style="width: 40px; height: 40px;">
                                            <i class="bi bi-person"></i>
                                        </div>
                                    }
                                    <div>
                                        <h5 class="mb-0">
                                            <a href="@Url.Action("Profile", "User", new { id = following.Id })" class="text-decoration-none">
                                                @following.DisplayName
                                            </a>
                                        </h5>
                                        <small class="text-muted">@@following.UserName</small>
                                    </div>
                                </div>

                                <div>
                                    @if (User.Identity?.IsAuthenticated == true && User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value != following.Id)
                                    {
                                        <form method="post" action="@Url.Action("Follow", "User", new { id = following.Id })" style="display: inline;">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-outline-danger">
                                                <i class="bi bi-person-x"></i> Bỏ theo dõi
                                            </button>
                                        </form>
                                    }
                                </div>
                            </div>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="alert alert-info" role="alert">
                    <i class="bi bi-info-circle"></i> Chưa theo dõi ai
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/lib/bootstrap/js/bootstrap.bundle.min.js"></script>
}
